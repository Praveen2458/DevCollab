# Client
# Optional. In dev, leave these unset to use the Vite proxy (/api + /socket.io)
# which keeps everything same-origin and makes HTTP-only auth cookies work reliably.
#
# VITE_API_URL=http://localhost:5000
# VITE_SOCKET_URL=http://localhost:5000
